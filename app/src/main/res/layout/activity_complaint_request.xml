<?xml version="1.0" encoding="utf-8"?>
<layout>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".ui.complaints.complaintRequest.ComplaintRequestActivity">


    <include
        android:id="@+id/addComplaintDataMainLayout"
        layout="@layout/activity_add_complaint_request_main"
        android:visibility="gone" />

    <ProgressBar
        android:id="@+id/addDataLoadingLayout"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_centerInParent="true"
        android:visibility="gone" />

    <include
        android:id="@+id/addComplaintDataErrorWithRetryLayout"
        layout="@layout/layout_info_lottie_with_retry"
        android:visibility="gone" />

    <include
        android:id="@+id/addComplaintDataErrorLayout"
        layout="@layout/layout_info_lottie"
        android:visibility="gone" />

    </RelativeLayout>
</layout>