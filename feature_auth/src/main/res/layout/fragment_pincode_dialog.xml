<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="10dp">
        <TextView
            android:id="@+id/pincodeTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="@font/raleway_black"
            android:text="Select Pincode"
            android:textSize="16sp"/>
        <androidx.appcompat.widget.SearchView
            android:id="@+id/pincodeSearchView"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:layout_below="@id/pincodeTitle"
            android:layout_marginTop="10dp"
            android:background="@color/searchBar"
            app:defaultQueryHint="Search Pincode"
            app:iconifiedByDefault="false"
            app:searchIcon="@drawable/search"/>
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/pincodeRecyclerView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/pincodeSearchView"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"/>
        <include
            android:id="@+id/progerssBar"
            layout="@layout/item_progress_bar"/>
    </RelativeLayout>

</layout>
